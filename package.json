{
  "name": "ultra-bun",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "lint": "turbo run lint",
    "format": "prettier . --write --cache",
    "format:check": "prettier . --check --cache",
    "check": "turbo run check-types lint && bun run format:check"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "configs/*",
      "packages/*",
      "scripts/*"
    ],
    "catalog": {
      "@types/bun": "^1.3.12",
      "eslint": "^10.2.0",
      "tsdown": "^0.21.7",
      "typescript": "^6.0.2",
      "zod": "^4.3.6"
    }
  },
  "devDependencies": {
    "prettier": "3.8.2",
    "turbo": "^2.9.6"
  },
  "packageManager": "bun@1.3.12"
}
